name: Build and Publish Docker Runtime

on:
  workflow_dispatch:
  pull_request:
    branches:
      - main
    paths:
      - "dockerfiles/Dockerfile-runtime"
      - "scripts/runtime/**"
      - "RUNTIME_IMAGE_VERSION"
      - ".github/workflows/docker-runtime.yml"
  push:
    branches:
      - main
    paths:
      - "dockerfiles/Dockerfile-runtime"
      - "scripts/runtime/**"
      - "RUNTIME_IMAGE_VERSION"
      - ".github/workflows/docker-runtime.yml"
    tags:
      - "*"

permissions:
  contents: read

jobs:
  docker-runtime:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to Docker Hub
        if: github.event_name == 'push'
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Read runtime image version
        id: runtime-version
        run: |
          set -euo pipefail
          runtime_version="$(tr -d '[:space:]' < RUNTIME_IMAGE_VERSION)"
          if [[ -z "${runtime_version}" ]]; then
            echo "RUNTIME_IMAGE_VERSION is empty." >&2
            exit 1
          fi
          echo "value=${runtime_version}" >> "$GITHUB_OUTPUT"

      - name: Generate image metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: companyhelm/runner
          tags: |
            type=raw,value=${{ steps.runtime-version.outputs.value }}
            type=ref,event=tag
            type=raw,value=latest,enable={{is_default_branch}}

      - name: Build and push runtime image
        uses: docker/build-push-action@v6
        with:
          context: .
          file: dockerfiles/Dockerfile-runtime
          platforms: linux/amd64,linux/arm64
          pull: true
          push: ${{ github.event_name == 'push' }}
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
